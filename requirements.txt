# FitPulse — Fitness Retention Intelligence Platform
# Python 3.11+ (validated on 3.13)

pandas>=2.1
numpy>=1.26
streamlit>=1.32
plotly>=5.20
pytest>=8.0
python-dotenv>=1.0
requests>=2.31

# Optional: report delivery / CI helpers
# (SQLite support ships with the Python standard library)
